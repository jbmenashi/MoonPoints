const initialState = {
   users: [],
   currentUserId: undefined
}

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default reducer